// not done in cpp

#include <bits/stdc++.h>
#include <sstream>
#include <algorithm>
#define ll long long
#define MAXN 100001
#define mod 1000000007
using namespace std;
#define pb push_back

int solve(string s)
{
    ll n = s.length(), l = 0, r = 0, ans = 0;
    string val;
    map<int, int> d;
    while (l < n && r < n)
    {
        val = s[r];
        // if (d[val]>=0){

        // }
    }
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    ll s;
    // vector<int> arr;
    // cin >> n;
    s = 'pwwkew';
    solve(s);
}
