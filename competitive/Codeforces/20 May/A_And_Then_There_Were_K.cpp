#include <bits/stdc++.h>
#include <sstream>
#define ll long long
#define MAXN 100001
#define mod 1000000007
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int t;
    cin >> t;
    while (t--)
    {
        ll x, a, b;
        // cin >> x >> a >> b;
        // for (ll int i = 0; i <n; i++)
        // {
        // //     ones_str = ones_str + '1';
        // }
        x = 5 && 4;
        a = x && 3;
        cout << "A" << x << endl;
    }
}
